version: '3.8'

services:
  md2t:
    build: .
    container_name: md2t
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD_HASH=${ADMIN_PASSWORD_HASH}
      - DOMAIN=${DOMAIN:-http://localhost}
      - BASE_URL=${BASE_URL:-http://localhost:5000}
      - DATABASE_URL=sqlite:///data/md2t.db
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # 可选：使用 Nginx 作为反向代理
  # nginx:
  #   image: nginx:alpine
  #   container_name: md2t-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #     - ./ssl:/etc/nginx/ssl
  #   depends_on:
  #     - md2t
